
  # Portfolio d'Architecture

  This is a code bundle for Portfolio d'Architecture. The original project is available at https://www.figma.com/design/qYm7Y5KrCQIBXy3GhbyHyW/Portfolio-d-Architecture.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  